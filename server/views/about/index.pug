extends ../layout/index

block content 
    div.header-default-space
    img.logo-img(src="./img/pen-man-logo.svg" width="50px" height="50px")
    h1.view-title=pageTitle
    
    h2.subtitle Herer you can see all the particular characteristic of our shop in a very new design and technology

    each tes in testimonials
        h4=tes.name
        p=tes.text

    br
    br
    br
    
    each item in items
        img.item-img(src=`./img/${item.img}`)
        p=item.title
        p=item.body